---
tags:
  - 日記
  - Notion
  - 自動化
created: 2026-09-11
---

# 自動日記の書き方

保存先はNotionの「自動日記」DB。1日＝1行。

## 何を書くか

作業日誌（DB_農作業記録）に載らないことだけを書く。一次データが作業日誌にあるものは書かない。

- 調べ物とその結論
- 道具の設定・自作・つまずいた点
- 考えごと、決めたことと理由
- 私事、人と話したこと
- AIとのやりとりで分かったこと

メモリに入らないものを優先する（調べた結果、試してダメだったこと、エラーの中身、判断の過程）。書くことが無い日は空の行のままにする。

## 行の探し方

```sql
SELECT "タイトル", url
FROM   "collection://75da553b-dfbd-422b-9273-acc45f4c6ece"
WHERE  date("date:日付:start") = '2026-08-27'
ORDER  BY createdTime
```

日付は文字列で直書きする（`?` は使わない）。本文はSQLでは返らないので `url` を `notion-fetch` する。

## タイトル

`YYYY-MM-DD（曜）トピック・トピック`

- 中心トピックを最大3つ、「・」で区切る（1つの日は1つ）
- 1トピックは**全角15字以内**。入らなければ短い言い方にする。説明ではなく名前にする
- **体言止め**。「〜した」「〜を検討」「〜まとめ」は付けない
- 「その他」「日記」「作業」のような中身の無い語や、日付の言い換えは入れない
- 拾えたものが無ければ `YYYY-MM-DD（曜）` のまま

良い例：`2026-08-27（水）八朔の摘果ゲージ・音声入力アプリ`
悪い例：`2026-08-27（水）八朔の摘果用53mmサイズゲージをFusionで設計しPETGで試作、あわせて連続音声入力の不具合を調査`

## 書式

- 常体
- トピックごとのH2見出し。見出しは案件名や物の名前をそのまま使う。先頭にタイトルと同じ見出しを置かない
- 中身が多いトピックは太字ラベルで割る
- 細かい出来事は `## 出来事` に集める。持ち越しがある日だけ `## 申し送り` を最後に置く
- 一つの箇条書きに一つの事実。感想と締めの一文は書かない
- 数字と固有名詞（型番、バージョン、パス、エラー、金額）を残す
- 判断の理由と、試してダメだったことを残す
- 改行は実際の改行で書く。`\n` を本文に入れない
- `23:47` `$20` をエスケープしない
- `睡眠時間[h]` と `睡眠評価` は触らない

## 日中のセッション

- 書く日付は常に当日（Asia/Tokyo）
- 当日の行は4:00のタスクが作ってある。探して追記するだけにする（無ければそのとき作る）
- 書くきっかけ：調べ物の結論が出た／理由があって何かを決めた／試してダメだったことが分かった／道具の設定を変えた・作った
- 当日の行を探すのは1セッションに1回。見つけたURLを使い回す
- 書き込みは `insert_content` の追記のみ。`replace_content` はしない
- タイトルは触らない
- 追記は1セッションに1〜2回
- このファイルを読み直さない

## 4:00のタスク

対象日は実行日 − 1日（Asia/Tokyo）。ツールが返す「今日」をそのまま使わない。

1. 対象日の行を探し、本文を読む。どのトピックが既に載っているかを控える。
2. 控えたものに無いトピックを材料から探す。
3. 見つかったぶんだけ追記する。既存の本文は消さない・書き換えない。
4. タイトルを確定する。
5. 当日（実行日）の行を空で作る。タイトルは `YYYY-MM-DD（曜）` のみ、`日付` は実行日、本文は空。同じ日付の行が既にあれば作らない。

材料：

- **メモリ** — 対象日に更新されたファイル
- **TickTick** — 対象日に完了・作成・更新されたタスク

材料の文をそのまま貼らず、その日の記述として書き直す。

守ること：

- 推測で埋めない。拾えたものが無ければ何も書かない。「特になし」だけの行にしない
- 対象日の行が無く、材料から書けることがあるときだけ新しく作る
- 実行時刻が4時台でなければ取り戻し実行を疑う。その場合も、材料に日付の裏付けがあるぶんだけをその日付の行に書く
