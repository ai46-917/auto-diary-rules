---
tags:
  - 日記
  - Notion
  - 自動化
created: 2026-09-11
---

# 自動日記の書き方

前日の行を補完して締め、当日の空の行を作る。本文は日中のセッションが書いている。ここでやるのは**書かれていないものを足すこと**で、書き直すことではない。

## 対象日

実行日 − 1日（Asia/Tokyo）。ツールが返す「今日」をそのまま使わない。

## 手順

1. 対象日の行を探し、本文を読む。どのトピックが既に載っているかを控える。
2. 控えたものに無いトピックを材料から探す。
3. 見つかったぶんだけ追記する。既存の本文は消さない・書き換えない。
4. タイトルを確定する。
5. 当日（実行日）の行を空で作る。タイトルは `YYYY-MM-DD（曜）` のみ、本文は空。同じ日付の行が既にあれば作らない。

## 行の探し方

```sql
SELECT "タイトル", url
FROM   "collection://75da553b-dfbd-422b-9273-acc45f4c6ece"
WHERE  date("date:日付:start") = '2026-08-27'
ORDER  BY createdTime
```

日付は文字列で直書きする（`?` は使わない）。本文はSQLでは返らないので `url` を `notion-fetch` する。

## 材料

- **過去チャット** — `recent_chats` で対象日のJST 0:00〜24:00。ISO文字列に `+09:00` を付ける。中身が要るものだけ開く（1回の実行で2件まで）。プロジェクト内の会話は取れない
- **メモリ** — 対象日に更新されたファイル
- **TickTick** — 対象日に完了・作成・更新されたタスク

材料の文をそのまま貼らず、その日の記述として書き直す。経緯が主眼なので、決めた理由や試してダメだったことを優先する。

## タイトル

`YYYY-MM-DD（曜）トピック・トピック`

- 中心トピックを最大3つ、「・」で区切る
- 1トピックは**全角15字以内**
- **体言止め**。「〜した」「〜を検討」は付けない
- 拾えたものが無ければ `YYYY-MM-DD（曜）` のまま

良い例：`2026-08-27（水）八朔の摘果ゲージ・音声入力アプリ`

## 書式

- 常体
- トピックごとのH2見出し。先頭にタイトルと同じ見出しを置かない
- 細かい出来事は `## 出来事` に集める。持ち越しがある日だけ `## 申し送り` を最後に置く
- 一つの箇条書きに一つの事実。感想と締めの一文は書かない
- 数字と固有名詞（型番、バージョン、パス、エラー、金額）を残す

## 守ること

- 推測で埋めない。拾えたものが無ければ何も書かない
- 改行は実際の改行で書く。`\n` を本文に入れない
- `23:47` `$20` をエスケープしない
- `睡眠時間[h]` と `睡眠評価` は触らない
